<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">
    
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/schedule_dialog_category_rv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:listitem="@layout/item_schedule_dialog_category"/>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/schedule_dialog_category_edit_cv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingVertical="5dp">
        
        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/schedule_dialog_category_edit_iv"
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:src="@drawable/ic_category_edit"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"/>

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:text="@string/category_edit"
            android:textSize="15sp"
            android:textColor="@color/textGray"
            app:layout_constraintStart_toEndOf="@id/schedule_dialog_category_edit_iv"
            app:layout_constraintTop_toTopOf="@id/schedule_dialog_category_edit_iv"
            app:layout_constraintBottom_toBottomOf="@id/schedule_dialog_category_edit_iv"
            app:layout_constraintEnd_toEndOf="parent" />
        
    </androidx.constraintlayout.widget.ConstraintLayout>
    
</LinearLayout>