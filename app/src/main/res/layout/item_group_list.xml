<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="group"
            type="com.mongmong.namo.domain.model.group.Group" />
    </data>

    <LinearLayout
        android:id="@+id/item_group_ll"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_category_view"
        android:orientation="horizontal"
        android:layout_marginBottom="20dp"
        android:padding="15dp">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/item_group_cover_img_iv"
            android:layout_width="45dp"
            android:layout_height="45dp"
            tools:src="@drawable/img_mongi_default" />

        <LinearLayout
            android:id="@+id/item_group_content_layout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginStart="15dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/item_group_title_tv"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{group.groupName}"
                android:textColor="@color/textGray"
                android:textSize="15sp"
                android:textStyle="bold" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="7dp"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/item_group_total_people_num_tv"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@{String.valueOf(group.groupMembers.size())}"
                    android:textColor="@color/textGray"
                    android:textSize="12sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/item_group_total_people_name_tv"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="10dp"
                    android:text="@{group.getMemberNames()}"
                    android:textColor="@color/textGray"
                    android:textSize="12sp" />

            </LinearLayout>
        </LinearLayout>
    </LinearLayout>
</layout>
